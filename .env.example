# Copy this file to ".env" and fill in your key. Never commit the real .env.
# Get a key at https://console.anthropic.com  (Billing → add a few $ of credit → API Keys → Create).
ANTHROPIC_API_KEY=sk-ant-...

# Optional — the model used for scoring. Sonnet is a good default for quality.
MODEL=claude-sonnet-5
